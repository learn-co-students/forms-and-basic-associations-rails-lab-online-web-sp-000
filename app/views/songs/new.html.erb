<h1>Create A Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <br />
  <%= f.text_field :title %>
  <br />
  <%= f.label :artist_name %>
  <br />
  <%= f.text_field :artist_name %>
  <br />
  <%= f.label :genre_id %>
  <br />
  <%= f.collection_select :genre_id, Genre.all, :id, :name %>
  <br />
  <label for="song[song_note]">For Notes</label>
  <input type="text" name="song[song_note][]" id="song_notes_1">
  <br />
  <label for="song[song_note]">For Notes</label>
  <input type="text" name="song[song_note][]" id="song_notes_2">
  <br />
  <%= f.submit %>
  <% end %>