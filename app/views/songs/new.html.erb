<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :song_title %>
  <%= f.text_field :title %>
  <%= f.label :song_artist_name %>
  <%= f.text_field :artist_name %>
  <%= f.collection_select :genre_id , Genre.all, :id, :name %>
  <input type="text" name="song[note_contents][]" id="song_notes_1">
  <input type="text" name="song[note_contents][]" id="song_notes_2">
  <input type="text" name="song[note_contents][]" id="song_notes_3">
  <%# <% <%= f.label :notes_id %>
  <%# <% <%= f.text_field :notes_id %>
  <%# <%= f.label :notes_id %>
  <%# <%= f.text_field :notes_id %>

  <%# <%= f.text_field :genre_name, list:
  "genres_autocomplete" %>
  <!-- <datalist id="genres_autocomplete"> -->
    <%# <% Genre.all.each do |genre| %>
      <%# <option value="<%= genre.name %>
    <%# <% end %>
  <!-- </datalist> -->
  <!-- <textarea name= "genre[name]"></textarea> -->
  <!-- <br /> --> 
<%= f.submit %>
<% end %>
