<h1>Create a Song</h1>

<%= form_for @song do |f| %>
<%= f.label :title %>
<%= f.text_field :title %>
<%= f.label :artist %>
<%= f.collection_select :genre_id, Artist.all, :id, :name %>
<%= f.label :genre %>
<%= f.collection_select :genre_id, Genre.all, :id, :name %>
<%= f.submit %>
<% end %>


