<%# This view should have a form that provides:
  - A text input box that sets the `Song`'s title.
  - A text input box for the `Artist`.
  - A selection box for `Genre`. Users should be able to 
    pick amongst existing genres only.
  - Several text input boxes to add notes to the song. %>

<h1>Create a Song</h1>

<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br />
  <%= f.label :artist_name %>
  <%= f.text_field :artist_name %>
  <br />
  <%= f.label :genre_name %>
  <%= f.collection_select :genre_id , Genre.all, :id, :name %>
  <%# <%= f.text_field :genre_id, list: 'genres_autocomplete' %>
  <%# <datalist id="genres_autocomplete"> %>
    <%# <% Genre.all.each do |genre| %>
      <%# <option value="<%= genre.name %>"
    <%# <% end %> 
  <%# </datalist> %>
  <br />
  <%= f.submit "commit" %>
<% end %>